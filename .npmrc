# NPM Configuration for NavEd
# Helps resolve installation issues with native dependencies

# Ignore scripts from dependencies to prevent build errors like 'bob' command not found
ignore-scripts=true

# Use legacy peer deps resolution to avoid conflicts
legacy-peer-deps=true

# Reduce verbosity of warnings
loglevel=error
